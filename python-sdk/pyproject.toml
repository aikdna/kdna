[build-system]
requires = []
build-backend = "backend"
backend-path = ["build_support"]

[project]
name = "aikdna"
version = "0.8.0rc2"
description = "Independent Python Core and Read for the exact KDNA public contract"
requires-python = ">=3.11"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
dependencies = []
