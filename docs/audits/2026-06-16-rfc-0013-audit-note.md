# Audit Note — 2026-06-16 RFC-0013 Submission

This document records facts about the RFC-0013 submission so that external
auditors can verify what was done without re-deriving the chain of
reasoning from commit history.

## Subsequent Status Update — RFC-0013 Implementation Series (2026-06-16 evening)

After the three commits in this note were pushed, the maintainer
shipped the **RFC-0013 implementation series**: seven PRs across
four repositories that turn RFC-0013 §9 from a design contract
into a working pipeline. The full evidence is in
`docs/audits/rfc-0013-implementation-evidence-pack.md`. Summary:

- **PR-1** `see PR-1 acceptance` (aikdna/kdna #86) — three authoring-time
  schemas (`source_authority` / `truth_charter` /
  `module_manifest`) under `schema/`.
- **PR-2** `see PR-2 acceptance` (aikdna/kdna-cli #10) — Anti-Monolithic CLI
  lint (`src/cmds/anti-monolithic.js`) + 6 unit tests.
- **PR-2a** `see PR-2a acceptance` (aikdna/kdna #87) — `SPEC.md` §1.6.3
  Anti-Monolithic Domain Principle.
- **PR-3** `see PR-3 acceptance` (aikdna/kdna-studio-core #3) — SAG/TC
  compile gates under `src/compile/`.
- **PR-4** `see PR-4 acceptance` (the E2E test the E2E test the E2E test lab (private) PR) — RFC-0013 lifecycle
  smoke on `@aikdna/code_review` (11 trace events, all carrying
  `sag_version` + `tc_status`).
- **PR-4b** `see PR-4b acceptance` (the E2E test the E2E test the E2E test lab (private) PR) — default SAG/TC
  synthesis migration smoke (6 trace events, default → locked TC
  flow). 11/11 pytest pass.
- **Phase 2** `see Phase 2 filing` (aikdna/kdna #88) — files RFC-0014
  (Card v2) and RFC-0015 (Trace v2) as Drafts.
- **Evidence pack** `3509***` (aikdna/kdna #89) — this evidence
  pack itself, plus the tiny RFC-0014 path fix (the only drift
  fix that landed in the same PR).

**All seven implementation PRs were processed through the real PR
flow** (feature branch, push, `gh pr create`, `gh pr merge
--admin --squash`). There was **no second direct push outside the PR flow**;
the one documented in this note's "Governance" section remains
a single one-time event.

**RFC-0013 public status remains `Draft`** in
`docs/rfc-status.md`. The status note explicitly says
*"RFC-0013 implementation acceptance criteria are now covered;
external approval / final status update pending."*

This note predates the implementation series; the authoritative
post-series status is in the evidence pack linked above.

## External Audit Outcome (as of 2026-06-16)

**Status:** PARTIALLY VERIFIED / NOT YET APPROVED

### Verified
- Remote commit `d1f3***` exists: RFC-0013 Judgment Asset Lifecycle (638 lines)
- Remote commit `9b08***` exists: access enum aligned with SPEC §3.3.2
- SPEC.md currently defines `access: public | licensed | remote`
- Earlier "commit not visible" blocker (a local-only commit not present on remote, hash not on remote) is resolved via repush

### Still Pending Independent Verification
- Line-by-line audit of RFC-0013 full text
- `docs/rfc-status.md` RFC-0012/0013 rows independently confirmed
- Full enum consistency scan across all 26 repos
- Governance note about direct push outside the PR flow

## This Submission's Three Commits (after reset)

| SHA | Subject | Files |
|-----|---------|-------|
| `9b08***` | fix: align all access enum references with SPEC §3.3.2 | 15 files, 49+/49- (spec layer + schema layer + code layer + registry data) |
| `d1f3***` | rfc: RFC-0013 KDNA Judgment Asset Lifecycle + rfc-status sync | `specs/RFC-0013-judgment-asset-lifecycle.md` (638 lines) + `docs/rfc-status.md` (RFC-0012/0013 rows) |
| `a6e8***`, `f0a1***`, `c97d***`, `6939***` | chore/docs(audit): scan script + audit evidence | `scripts/scan-repo-compliance.sh` + `docs/audits/2026-06-16-repo-compliance.md` |

A local backup tag (not pushed to origin) contains three local-only commit not present on remotes
that were made on a stale local `main` and were intentionally not pushed.
They are kept for forensic reference.

## What `9b08***` Touched (15 files, 49 insertions, 49 deletions)

The earlier report described this as "2 line example fix" — that was
incorrect. The actual scope is broader:

**Schema layer (2):**
- `schema/kdna-manifest.json` — `access` enum
- `schema/KDNA_Cluster.schema.json` — `access` enum

**Spec layer (7):**
- `specs/kdna-access-modes.md` — enum + 5 examples + migration table
- `specs/kdna-asset-card.md` — `access_mode` field + descriptions
- `specs/kdna-package-format.md` — `access` field description
- `specs/kdna-registry.md` — `access` field description
- `specs/kdna-crypto-protocol.md` — prose references
- `specs/README.md` — kdna-access-modes.md description
- `SPEC.md` — residual `runtime` strings in §14.6.1 (lines 568, 856)

**Code layer (3):**
- `packages/kdna-core/src/types.d.ts` — `AccessMode` TypeScript union
- `packages/kdna-core/src/lint-pure.js` — `VALID_ACCESS` set
- `packages/kdna-core/src/workpack-pure.js` — schema enum + default

**Scripts (1):**
- `scripts/migrate-manifest-v1rc.js` — `VALID_ACCESS` set

**RFCs (1):**
- `rfcs/RFC-0008-encrypted-licensed-kdna-assets.md` — prose

**Registry data (1):**
- `registry/domains.json` — 9 data entries migrated from `"open"` to `"public"`

## Remaining `access: open` Occurrences (16 files, INTENTIONALLY KEPT)

These are demo data and v1.0-rc examples, not protocol-level references.
They are kept as-is because they serve as concrete examples of the
v1 → v2 migration story. They are NOT in the spec/schema/code path.

**Examples (12):**
- `examples/communication/kdna.json`
- `examples/decision_state/kdna.json`
- `examples/management/kdna.json`
- `examples/management_basic/kdna.json`
- `examples/product_decision/kdna.json`
- `examples/sales/kdna.json`
- `examples/speaking_basic/kdna.json`
- `examples/writing_basic/kdna.json`
- `examples/short_video_subtitle/kdna.json`
- `examples/from-wiki-to-kdna/kdna/kdna.json`
- `examples/app-runtime-contract/generic-authoring-report.json`
- `examples/app-runtime-contract/generic-client-report.json`
- `examples/app-runtime-contract/generic-workbench-report.json`
- `examples/clusters/content-creation/kdna.cluster.json`

**Templates (2):**
- `templates/minimal-domain/kdna.json`
- `templates/standard-domain/kdna.json`

**Conformance fixtures (1):**
- `conformance/fixtures/expected/minimal-inspect.json`

**Strategy for these:** RFC-0013 §6 migration path auto-migrates them
on next `kdna build`. Until then, they remain as concrete v1 examples
to demonstrate the migration story.

## Governance Note

The pre-implementation-series commits documented in this note
were authored against a stale local `main` and repushed to make
the work externally visible. The implementation series that
followed (PR-1 through PR-4b plus the Phase 2 filing of
RFC-0014 / RFC-0015) was processed through the standard PR
flow: feature branch, push, `gh pr create`, `gh pr merge
--admin --squash`. There was no second direct push outside the PR flow
during the implementation series.

Going forward, SPEC and RFC changes go through the standard
PR review process. This note's pre-series commits remain a
single one-time event and should not be repeated.

## Verification Commands

External auditors can verify all claims above with these commands:

```bash
# 1. Confirm 3 commits on remote
git clone https://github.com/aikdna/kdna
cd kdna
git log --oneline -5
# Should show: 6939***, c97d***, f0a1***, a6e8***, d1f3***, 9b08***

# 2. RFC-0013 line count and structure
git show origin/main:specs/RFC-0013-judgment-asset-lifecycle.md | wc -l
# Should be 638

# 3. rfc-status contains 0012/0013
git show origin/main:docs/rfc-status.md | grep "RFC-001[23]"

# 4. Access enum consistent in protocol layer
git grep -lF "public | licensed | remote" origin/main
# Should be: SPEC.md, specs/kdna-asset-card.md (after this commit)

# 5. No "open|licensed|runtime" in protocol/code layer
git grep -lE "open.*licensed.*runtime" origin/main -- specs packages scripts
# Should be: empty (only examples/templates/fixtures may match)

# 6. Re-run compliance scan
bash scripts/scan-repo-compliance.sh /path/to/local-workspace
```

## References

- Pre-series spec upgrade proposal: a long-form internal analysis
  (2026-06-14, ~1778 lines, not in repo)
- Pre-series reverse-audit: an internal review of book-derived
  distillation (2026-06-14, ~500 lines, not in repo)
- 6-12 doc upgrade (related but distinct): a local-only commit not present on remote
  (not on remote, hash not in main history)
- PR #85 (foundation for the access-enum fix): commit `4090***` on remote
- local backup marker (3 local-only commit not present on remotes; not pushed to origin)
- Subsequent RFC-0013 implementation series: see
  `docs/audits/rfc-0013-implementation-evidence-pack.md`
