# Studio Export → v1 Container E2E Status — June 2026

## Verdict: NPM REGISTRY CLEAN-INSTALL PASS

`kdna-studio migrate --format v1` now produces KDNA Core v1 containers for the
three flagship source assets from public npm packages. The path passes
source-tree verification, local tarball clean-install verification, and npm
registry clean-install verification, including validate/load and major
runtime-section preservation.

Read this together with:

- `docs/audits/flagship-v1-migration-blocker-summary.md`
- `docs/audits/flagship-writing-v1-migration-fidelity.md`
- `docs/audits/flagship-agent-safety-v1-migration-fidelity.md`
- `docs/audits/flagship-prompt-diagnosis-v1-migration-fidelity.md`

Net release verdict: the Studio producer path is no longer blocked by npm
packaging or install-path issues. Public flagship release still requires final
artifact acceptance, README updates, and cross-repo launch reconciliation.

## E2E chain verified

```
kdna-studio create project
  → Add judgment cards (axioms)
kdna-studio migrate project --format v1 --out asset.kdna
  → @aikdna/kdna-core@0.11.1 pack/checksum helper
kdna validate asset.kdna
  → format/schema/payload/checksums/load-contract gates pass in local source verification
kdna load asset.kdna --profile=compact --as=prompt
  → Agent-readable judgment text
```

## Implementation

- `kdna-studio-cli/bin/kdna-studio.js` — v1 path added to `cmdMigrate`
- v1 export skips v2 compile gate and calls `@aikdna/kdna-core` directly
- Manifest: source / Studio project metadata → v1 manifest.schema.json fields
- Payload: locked Studio cards → JSON (not CBOR)
- Packaging: official `core.pack` (deterministic ZIP over files already present)
- Validation: official `core.validate` (format + schema + payload + checksums + load-contract)

## Current npm registry verification

- Scoped package-name to v1 `asset_id` mapping is present:
  `@aikdna/writing` -> `kdna:aikdna:writing`.
- `checksums.json` is generated by the Studio v1 export path.
- The full profile preserves `payload.source_cards` and major runtime
  sections for `writing`, `agent_safety`, and `prompt_diagnosis`.
- Compact prompt output renders object patterns as readable text.
- Compact prompt output includes derived boundaries for the three flagship
  assets.
- `kdna-studio-cli npm test`: 18/18 pass.
- `kdna-studio-core npm test`: 128/128 pass.
- `kdna npm test`: pass.
- Local tarball clean install in `/tmp/kdna-clean-proof-2` passes for:
  `writing`, `agent_safety`, and `prompt_diagnosis`.
- npm registry clean install in `/tmp/kdna-registry-proof` passes with:
  - `@aikdna/kdna-core@0.11.1`
  - `@aikdna/kdna-cli@0.25.0`
  - `@aikdna/kdna-studio-core@1.5.3`
  - `@aikdna/kdna-studio-cli@0.5.2`
  - `@aikdna/kdna@0.9.0`
- Registry proof exported and validated:
  - `writing.kdna`: `overall_valid: true`, `source_cards: 35`, `axioms: 4`, `boundaries: 10`, `patterns: 17`, `self_checks: 5`, `failure_modes: 3`
  - `agent_safety.kdna`: `overall_valid: true`, `source_cards: 29`, `axioms: 3`, `boundaries: 8`, `patterns: 14`, `self_checks: 5`, `failure_modes: 2`
  - `prompt_diagnosis.kdna`: `overall_valid: true`, `source_cards: 30`, `axioms: 3`, `boundaries: 8`, `patterns: 15`, `self_checks: 5`, `failure_modes: 2`
- Compact prompt output is readable for all three assets.

## What's NOT included in v1 export

- quality_badge, Human Lock status, risk levels as v1 manifest claims
- legacy reports (KDNA_CARD, quality-gate-report, provenance-report, etc.)
- CBOR-encoded payload (v1 uses JSON)
- v2 mimetype (v1 uses `application/vnd.kdna.asset`)

## Remaining ecosystem limitations

- Updated per-asset fidelity reports are still pending. The older reports record
  the pre-hardening 10-11% fidelity failure and should not be used as the final
  launch evidence.
- Website, MCP package publication, public README alignment, and issue
  reconciliation still need final launch closure.
