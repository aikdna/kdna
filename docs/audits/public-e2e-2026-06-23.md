# Public E2E Audit — 2026-06-23

> **Purpose**: Verify that the KDNA Core toolchain can be installed and run end-to-end
> on a clean environment, covering the full path: demo → pack → validate → plan-load → load,
> plus validation of all published kdna-x assets.

## Environment

| Component | Version |
|-----------|---------|
| kdna-cli | 0.27.4 |
| @aikdna/kdna-core | (bundled) |
| Node.js | v22.22.3 |
| npm | 10.9.8 |
| OS | macOS (darwin) |

## Test 1: Minimal Fixture Chain

```
kdna demo minimal /tmp/minimal-source
```

**Result: PASS** — Created `checksums.json`, `kdna.json`, `mimetype`, `payload.kdnab`.
Container format verified: no source-tree JSON files at ZIP top level.

```
kdna pack /tmp/minimal-source /tmp/minimal.kdna
```

**Result: PASS** — Packed 4 entries. Container type: KDNA Asset Container.

```
kdna validate /tmp/minimal.kdna
```

**Result: PASS** — `overall_valid: true`.

```
kdna plan-load /tmp/minimal.kdna
```

**Result: PASS** — `state: ready`, `can_load_now: true`, 0 issues.

```
kdna load /tmp/minimal.kdna --profile=compact --as=prompt
```

**Result: PASS** — Rendered compact prompt with axioms, self-checks, failure modes.
Output includes safety boundary, token hint, and structured judgment content.

## Test 2: kdna-x Published Assets

| Asset | validate | plan-load | can_load | Issues |
|-------|----------|-----------|----------|--------|
| viral-topic-selection.kdna | ✅ valid | ✅ ready | true | 0 |
| title-attraction.kdna | ✅ valid | ✅ ready | true | 0 |
| short-video-script.kdna | ✅ valid | ✅ ready | true | 0 |

All 3 published assets pass the full chain without errors.

## Test 3: Load Output Verification

`viral-topic-selection.kdna --profile=compact --as=prompt` produces:
- 4 axioms with applies_when / does_not_apply_when / failure_risk
- 3 boundary definitions with scope / out_of_scope / acceptable_exceptions
- 6 self-check questions
- 3 named failure modes with corrective guidance
- 2 anti-patterns flagged

Output is well-structured, domain-specific, and demonstrates clear judgment guidance.

## Issues Found

None. All commands succeed with clean output.

## Conclusion

**E2E Gate: PASSED**

The KDNA Core toolchain is functional end-to-end. The demo→pack→validate→plan-load→load chain
completes without errors. All 3 published kdna-x assets validate and load correctly.
The toolchain is ready for asset production.
